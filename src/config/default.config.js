/** ----------------------------------------
    Event
 ---------------------------------------- */

import Emit from '@util/helper/Emit';

/** ----------------------------------------
    Construct
 ---------------------------------------- */

const setDefaults = config => {

    /** ----------------------------------------
        Configuration
     ---------------------------------------- */

    const defaults = [

        /** ----------------------------------------
            Model
         ---------------------------------------- */

        {
            name: 'models',
            value: {}
        },

        /** ----------------------------------------
            Timing
         ---------------------------------------- */

        {
            name: 'now',
            value: Date.now()
        },

        {
            name: 'date',
            value: null
        },

        {
            name: 'time',
            value: null
        },

        /** ----------------------------------------
            Routine
         ---------------------------------------- */

        {
            name: 'chain',
            value: []
        },

        /** ----------------------------------------
            Routine
         ---------------------------------------- */

        {
            name: 'routine',
            value: 'once',
        },

    ];

    return defaults;
};

/** ----------------------------------------
    Exports
 ---------------------------------------- */

export default setDefaults;