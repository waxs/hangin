/** ----------------------------------------
    ID
 ---------------------------------------- */

const id = prefix => {
    return (prefix && prefix + '_') || '' + Math.random().toString(16).substr(2, 9);
};

/** ----------------------------------------
    Helpers
 ---------------------------------------- */

export default id;